import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'app-ufma',
  webDir: 'dist/app-ufma/browser', // Caminho atualizado corretamente
};

export default config;
